apiVersion: v1
kind: Service
metadata:
  name: ninaivalaigal-api-service
  labels:
    app: ninaivalaigal-api
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 13370
    protocol: TCP
    name: http
  selector:
    app: ninaivalaigal-api
