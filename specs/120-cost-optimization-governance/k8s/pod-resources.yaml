apiVersion: v1
kind: Pod
metadata:
  name: nv-api
spec:
  containers:
  - name: api
    image: ghcr.io/medhasys/ninaivalaigal-api:dev
    resources:
      requests:
        cpu: "250m"
        memory: "256Mi"
      limits:
        cpu: "1"
        memory: "512Mi"
