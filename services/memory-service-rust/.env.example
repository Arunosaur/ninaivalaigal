# Memory Service (Rust) Configuration
# Copy to .env and update with your values

# Database (via PgBouncer)
DATABASE_URL=postgresql://nina:dev_password_change_in_production@localhost:6432/ninaivalaigal_dev

# Redis
REDIS_URL=redis://localhost:6379/0

# JWT Authentication (must match Core API)
NINAIVALAIGAL_JWT_SECRET=your-secret-key-here

# Logging
RUST_LOG=info,memory_service=debug

# Server (internal port, mapped externally)
# Internal: 8000
# External (Apple Dev): 13393
