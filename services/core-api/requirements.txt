# Core API Service Dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0  # SPEC-107: Process manager for runtime parity
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0

# Shared Contracts (SPEC-100 Task #79)
# Installed separately in Dockerfile: pip install -e /app/shared/contracts
# DO NOT install via requirements.txt - path context breaks in Docker build

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1
asyncpg==0.29.0
pgvector==0.2.4

# Authentication
pyjwt==2.8.0
passlib==1.7.4
bcrypt==4.1.1
python-multipart==0.0.6
# SPEC-065: Multi-Factor Authentication
pyotp==2.9.0  # TOTP generation and verification
qrcode[pil]==7.4.2  # QR code generation for TOTP enrollment
webauthn==1.2.0  # WebAuthn/FIDO2 support
# SPEC-065: Single Sign-On (SSO)
python3-saml==1.15.0  # SAML 2.0 support
authlib==1.2.1  # OAuth2 and OpenID Connect support
cryptography==41.0.7  # Certificate handling

# Logging
structlog==23.2.0

# Monitoring & Alerting (US#361)
aiohttp==3.9.1
prometheus-client==0.19.0

# Utilities
python-dateutil==2.8.2

# Monitoring & Metrics (SPEC-100)
psutil==5.9.6
redis==5.0.1

# SPEC-117: Feature Flags & Progressive Rollout
UnleashClient==5.8.0  # Unleash Python SDK for feature flags

# HTTP Client & Additional Dependencies
httpx==0.25.2
requests==2.31.0

# Cryptography for API keys
cryptography==41.0.7

# HSM Integration (SPEC-065, US#623)
boto3==1.34.0  # AWS CloudHSM/KMS
azure-keyvault-keys==4.8.0  # Azure Key Vault
azure-identity==1.15.0  # Azure authentication
google-cloud-kms==2.20.0  # Google Cloud KMS (latest published release compatible with SPEC-145 build matrix)

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0
httpx==0.25.2  # For TestClient

# LangSmith Observability (US#139)
langsmith==0.1.129
python-dotenv==1.0.0
