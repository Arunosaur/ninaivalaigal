version: '3.8'

# Apple Container CLI - Development Environment
# Optimized for M1/M2 Macs with native ARM performance
# Usage: docker-compose -f compose.apple.dev.yml up -d

services:
  postgres:
    image: postgres:15.8
    platform: linux/arm64
    container_name: ninaivalaigal-apple-dev-db
    environment:
      POSTGRES_DB: ninaivalaigal_dev
      POSTGRES_USER: nina
      POSTGRES_PASSWORD: ${NINA_DB_PASSWORD:-secure_nina_password}
      POSTGRES_HOST_AUTH_METHOD: md5
    ports:
      - "5452:5432"  # Apple/Dev port from matrix
    volumes:
      - ./data/postgres_dev:/var/lib/postgresql/data  # Bind mount - shared with docker and colima
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nina -d ninaivalaigal_dev"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - ninaivalaigal_apple_dev_network

  redis:
    image: redis:7.4.0-alpine
    platform: linux/arm64
    container_name: ninaivalaigal-apple-dev-redis
    ports:
      - "6399:6379"  # Apple/Dev port from matrix
    environment:
      REDIS_PASSWORD: ${NINA_REDIS_PASSWORD:-secure_nina_password}
    command: redis-server --requirepass ${NINA_REDIS_PASSWORD:-secure_nina_password} --maxmemory 1gb --maxmemory-policy allkeys-lru
    volumes:
      - ./data/redis_dev:/data  # Bind mount - shared with docker and colima
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${NINA_REDIS_PASSWORD:-secure_nina_password}", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    restart: unless-stopped
    networks:
      - ninaivalaigal_apple_dev_network

  api:
    build:
      context: .
      dockerfile: Dockerfile.api
      platforms:
        - linux/arm64
    platform: linux/arm64
    container_name: ninaivalaigal-apple-dev-api
    command: ["python", "run_server.py"]
    ports:
      - "13390:8000"  # Apple/Dev port from matrix
    environment:
      NINAIVALAIGAL_DATABASE_URL: postgresql://nina:${NINA_DB_PASSWORD:-secure_nina_password}@postgres:5432/ninaivalaigal_dev
      NINAIVALAIGAL_JWT_SECRET: ${NINA_JWT_SECRET:-dev_jwt_secret}
      NINAIVALAIGAL_DEBUG: "1"
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${NINA_REDIS_PASSWORD:-secure_nina_password}
      REDIS_DB: 0
      PYTHONPATH: /app/server
      PORT: 8000
      UVICORN_RELOAD: "0"
      ENVIRONMENT: development
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./server:/app/server:ro
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped
    networks:
      - ninaivalaigal_apple_dev_network

  ui:
    build:
      context: ./ui
      dockerfile: Dockerfile
      platforms:
        - linux/arm64
    platform: linux/arm64
    container_name: ninaivalaigal-apple-dev-ui
    ports:
      - "8101:8081"  # Apple/Dev port from matrix
    environment:
      NODE_ENV: development
      VITE_API_URL: http://localhost:13390
    depends_on:
      - api
    restart: unless-stopped
    networks:
      - ninaivalaigal_apple_dev_network

# No volumes section needed - using bind mounts

networks:
  ninaivalaigal_apple_dev_network:
    driver: bridge
