# Colima Runtime Configuration
# For Colima container runtime on macOS

NINA_RUNTIME=colima
RUNTIME_OFFSET=10
CONTAINER_COMMAND=docker
PLATFORM=linux/arm64  # Colima supports both arm64 and amd64
ARCHITECTURE=arm64

# Colima specific settings
COLIMA_PROFILE=default
